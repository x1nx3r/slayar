# Replace with a real key before applying:
# kubectl create secret generic laya-router-api-key --from-literal=apiKey=$(openssl rand -hex 32) --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: laya-router-api-key
stringData:
  apiKey: changeme
